# 🚀 Minimal Requirements for Railway (if JAX fails)
# Emergency fallback without JAX

# Core Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Environment
python-dotenv>=1.0.0

# Basic ML (fallback)
scikit-learn>=1.3.0

# Alternative start commands for Railway deployment

# Option 1: Standard Gunicorn (RECOMMENDED)
web: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload

# Option 2: With more workers (for higher traffic)
# web: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --preload

# Option 3: Development mode (only for testing)  
# web: python app.py

# Option 4: Waitress (alternative to Gunicorn)
# web: waitress-serve --host=0.0.0.0 --port=$PORT app:app
